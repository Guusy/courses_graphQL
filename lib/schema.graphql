type Course {
    _id: ID
    title: String
    teacher: String
    description: String
    topic: String
}

type Query {
    "Return all the courses"
    getCourses: [Course]
    "Course by id"
    getCourse( id: ID!): Course
}